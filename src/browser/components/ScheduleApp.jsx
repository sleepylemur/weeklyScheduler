var PriorityList = require('./PriorityList.jsx');

module.exports = React.createClass({
  render: function() {
    return (
      <div className="ScheduleApp">
        <PriorityList />
      </div>
    );
  }
});
